<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}">CardGate</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <ul class="navbar-nav me-auto mb-2 mb-lg-0">    

        {% if app.user %}

            {% if app.user.roles[0] is same as "ROLE_ADMIN" %}
        
              <li class="nav-item">
                <a class="nav-link" aria-current="page" href="{{ path('admin_home') }}">Administration</a>
              </li>

            {% endif %}

        {% endif %} 

        {% for category in categoryRepository.findAll() %}
          
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('boutique_product_show_by_category',{'id':category.id}) }}">
                {{ category.name }}
              </a>
            </li>

        {% endfor %}
        
      </ul>

      <div class="d-flex">

        {% if app.user %}
          
          <a class="nav-link text-white" aria-current="page" href="{{ path('app_logout') }}">Log out <i class="fa-solid fa-arrow-right-from-bracket"></i></a>
          <a class="nav-link text-white" aria-current="page" href="{{ path('profile_detail') }}">My account <i class="fas fa-user"></i></a>

        {% else  %}
          
            <a class="nav-link text-white" aria-current="page" href="{{ path('app_login') }}">Log in <i class="fa-solid fa-arrow-right-to-bracket"></i></a>

            <a class="nav-link text-white" aria-current="page" href="{{ path('app_register') }}">Register <i class="fa-solid fa-arrow-up-from-bracket"></i></a>
          
        {% endif %}
          
        <a class="nav-link text-white" aria-current="page" href="{{ path('cart_detail') }}">
          My cart <i class="fas fa-shopping-bag"></i>
        </a>

      </div>
      
    </div>
  </div>
</nav>